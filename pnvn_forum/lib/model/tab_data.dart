class TabData {
  String id;
  String title;
  bool isActive;
  int index;
  bool isLastItem;
  TabData(
      {required this.id,
      required this.title,
      required this.isActive,
      required this.index,
      required this.isLastItem});
}
